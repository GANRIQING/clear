server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/zxb?serverTimezone=UTC&useUnicode=true&characterEncoding=utf8&useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html
    encoding: UTF-8
    mode: LEGACYHTML5
    servlet:
      content-type: text/html
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
myFile:
  staticAccessPath: /upload/**
